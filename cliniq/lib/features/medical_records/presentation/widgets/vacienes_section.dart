import 'package:flutter/material.dart';

class VacienesSection extends StatelessWidget {
  const VacienesSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Vacienes"),
    );
  }
}