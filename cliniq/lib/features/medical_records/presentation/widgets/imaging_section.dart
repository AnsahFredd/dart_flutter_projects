import 'package:flutter/material.dart';

class ImagingSection extends StatelessWidget {
  const ImagingSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("IMAGEIG SECTION"),
    );
  }
}